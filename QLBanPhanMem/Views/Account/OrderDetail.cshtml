@model QLBanPhanMem.Models.OrderDetailViewModel;

@{
    ViewBag.Title = "Lịch sử đơn hàng";
    Layout = "_Layout";
}
<nav style="--bs-breadcrumb-divider: '>'; padding-top:20px" aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="/Home/Index">Trang chủ</a></li>
        <li class="breadcrumb-item"><a href="/Account/Edit/@ViewBag.uid">Tài khoản</a></li>
        <li class="breadcrumb-item active" aria-current="page">Lịch sử đơn hàng</li>
    </ol>
</nav>
<div class="row">
    <div class="col-md-4">
        @* <ul class="list-group">
            <li class="list-group-item" aria-current="true"><a href="/Account/Edit/@Model.accountModel.Uid">Tài khoản</a></li>
            <li class="list-group-item active" aria-current="true"><a href="/Account/History/@Model.accountModel.Uid">Lịch sử đơn hàng</a></li>
            <li class="list-group-item" aria-current="true"><a href="/Account/ChangePassword/@Model.accountModel.Uid">Mật khẩu</a></li>
            <li class="list-group-item" aria-current="true"><a asp-action="FavoriteProduct" asp-controller="Account">Sản phẩm yêu thích</a></li>
            <li class="list-group-item" aria-current="true"><a asp-action="ShareWebsite" asp-controller="Account">Giới thiệu bạn bè</a></li>
        </ul> *@
    </div>
    <div class="col-md-8" style="padding:30px">
        <h5>Chi tiết đơn hàng</h5>
        <a>Hiển thị chi tiết đơn hàng</a>
        <br />
        <br />
        <table class="table table-striped">
            <thead>
            <tr>
                <th scope="col">Phần mềm</th>
                
                <th scope="col">Thành tiền</th>
                    
            </tr>
            </thead>
            <tbody>

            @foreach (var item in Model.chiTietHoaDonModel)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.PhanMem.TENPM)
                    </td>
                    
                        
                    <td>
                        @Html.DisplayFor(modelItem => item.THANHTIEN) ₫
                    </td>
                        
                </tr>
            }

        </table>
        <br />
        <table class="table table-striped">
            <thead>
                <tr>
                    <th scope="col">Phần mềm</th>
                    <th scope="col">Khóa kích hoạt</th>
                    <th scope="col">Tài khoản</th>
                    <td scope="col">Mật khẩu</td>
                </tr>
            </thead>
            <tbody>

                @foreach (var item in Model.cthdKeyModel)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.PhanMem.TENPM)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.KEYPM.GIATRI)

                        </td>

                        <td>
                            @Html.DisplayFor(modelItem => item.KEYPM.TAIKHOAN)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.KEYPM.MATKHAU)
                        </td>

                    </tr>
                }

        </table>
    </div>
</div>

